<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ewiderbuy.produce.dao.CustomDao" >
    <!-- 查询条件 -->
    <select id="customSelect" parameterType="string" resultType="java.util.LinkedHashMap" >
        ${_parameter}
    </select>

    <update id="customUpdate" parameterType="string" >
        ${_parameter}
    </update>

    <insert id="customInsert" parameterType="string" >
        ${_parameter}
    </insert>
</mapper>